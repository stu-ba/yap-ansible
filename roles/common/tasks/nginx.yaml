---

- name: install nginx
  become: yes
  apt: 
    name: nginx 
    state: present
  notify: start nginx
  tags: [apt]

- name: disable default config
  become: yes
  file: 
    dest: /etc/nginx/sites-enabled/default
    state: absent
  notify: 
    - reload nginx
